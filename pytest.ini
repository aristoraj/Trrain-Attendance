[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Don't run tests that need InsightFace loaded (heavy model) in CI unless explicitly requested
markers =
    slow: marks tests as slow (require real face model)
    integration: marks integration tests requiring Zoho credentials
